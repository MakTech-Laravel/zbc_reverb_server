services:
  reverb:
    build: .
    ports:
      - "${REVERB_SERVER_PORT:-8080}:8080"
    env_file:
      - .env
    environment:
      REVERB_SERVER_HOST: 0.0.0.0
      REVERB_SERVER_PORT: 8080
    restart: unless-stopped
